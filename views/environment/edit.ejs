<!DOCTYPE html>
<html ng-app="EnvironmentModule" ng-controller = "EnvironmentController" ng-cloak>
  <head>
    <title>WPT : {{title}}</title>

    <!-- Viewport mobile tag for sensible mobile support -->
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <!--STYLES-->
    <link rel="stylesheet" href="/styles/bootstrap-theme.min.css">
    <link rel="stylesheet" href="/styles/bootstrap.min.css">
    <link rel="stylesheet" href="/styles/dataTables.css">
    <link rel="stylesheet" href="/styles/importer.css">
    <!--STYLES END-->
    <!--SCRIPTS-->
    <script src="/js/dependencies/sails.io.js"></script>
    <script src="/js/dependencies/jquery-1.12.3.min.js"></script>
    <script src="/js/dependencies/bootstrap.js"></script>
    <script src="/js/dependencies/angular.min.js"></script>
    <script src="/js/dependencies/dataTables.min.js"></script>
    <script src="/js/app.js"></script>
    <script src="/js/public/environment/EnvironmentModule.js"></script>
    <script src="/js/public/environment/EnvironmentController.js"></script>
    <!--SCRIPTS END-->

  </head>
<body>
<fieldset>
<legend><h1>Edit Environment</h1></legend>
 <div class="container-fluid">
    <form ng-submit="editEnvironmentForm()"  id="edit-environment-form" name="editEnvironment">
    
        <!-- N A M E -->


        <div class="control-group form-group col-md-12" ng-class="{'has-error':editEnvironment.name.$invalid && editEnvironment.name.$dirty}">
          <label>Name</label>

          <!-- For this input field make it required, and have a max length of 25 -->
          <input type="text" class="form-control" placeholder="e.g. Production" name="name" ng-model="editEnvironmentForm.name" ng-maxlength="50" required>


          <span class="help-block has-error" ng-if="editEnvironment.name.$dirty">
            <span ng-show="editEnvironment.name.$error.required">Name is required.</span>
            <span ng-show="editEnvironment.name.$error.maxlength">The name cannot be more than 50 characters.</span>
          </span>
        </div>

        <!-- Disable signup button until the form has no errors -->
      <button class="btn btn-primary btn-lg btn-block" type="submit" ng-disabled="editEnvironment.$invalid">
        <span ng-hide="editEnvironmentForm.loading">Edit Environment</span>
        <span class="overlord-loading-spinner fa fa-spinner" ng-show="editEnvironmentForm.loading" ></span>
        <span ng-show="editEnvironmentForm.loading">Editing your environment...</span>
      </button>

      <!--<input type="hidden" name="_csrf" value="<%= _csrf %>" />-->

    </form>
 </div>
 </fieldset> 

</body>
</html>